# clawpi-tools

## Version Bumps

Every time you bump the version in `openclaw.plugin.json` and `package.nix`, also add a new entry to `CHANGELOG.md` describing what changed. Keep entries concise — one line per change.
